version: '3.3'

services:
  reverse-proxy:
    build: .
    container_name: reverse-proxy
    ports:
      - "80:80"
      - "443:443"
    networks:
      - app-network
    depends_on:
      - frontend
      - backend

networks:
  app-network:
    external: true